// User function template for JAVA
Link:https://www.geeksforgeeks.org/problems/pangram-checking-1587115620/1?page=1&difficulty=Easy&sortBy=submissions
class Solution {
    // Function to check if a string is Pangram or not.
    public static boolean checkPangram(String s) {
        // your code here
        for(char ch ='a';ch<='z';ch++){
            boolean found=false;
            for(int i=0;i<s.length();i++){
                if(ch == Character.toLowerCase(s.charAt(i))){
                    found=true;
                    break;
                }
            }
            if(found==false){
                return false;
            }
            
        }
        
        return true;
        
    }
}
